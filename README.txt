Project description:

A basic(dockerized) C++ project that, given a polyline and a query point, computes the closest point on the polyline to the query point.

Usage:
 docker build -t ct . && docker run -it ct
